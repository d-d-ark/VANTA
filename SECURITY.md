# Security

이 저장소는 종료된 VANTA 기능의 공개 아카이브입니다. 운영 배포 전 다음 경계를 직접 강화하세요.

- `config.example.php`의 모든 DB·Firebase·HMAC 값을 자신의 비밀값으로 교체하고 저장소에 커밋하지 않습니다.
- 서비스 계정은 필요한 Realtime Database 범위로만 제한하고 키를 정기적으로 교체합니다.
- Live Cursor 직접 Firebase 모드는 서버 쿼터를 완전히 강제할 수 없으므로 기본적으로 비활성화하거나 Firebase 예산·알림·App Check 등 별도 통제를 둡니다.
- 페이지 컨텍스트 브리지는 신뢰하지 않는 제3자 스크립트가 함께 실행되는 환경에서 보안 경계로 사용하지 않습니다.
- 평문 FTP로 설정이나 자격 증명을 배포하지 않습니다. SFTP, HTTPS 기반 CI 또는 호스팅 제공자의 비밀 저장소를 사용합니다.

실제 키, 토큰, 사용자 데이터와 로그를 공개 이슈에 첨부하지 마세요.
